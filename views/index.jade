extends layout

block content
  h1 Find your train!
  h3 Announcement: There is Not Train-- in 2023..Lets join ..in 2025....Let Board the Train in Platform 1 .. in Jan..
  p Select your train below to see its current schedule.
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInfo
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.
